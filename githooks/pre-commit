#!/bin/sh
# pre-commit.sh

# Add commands here to run before each commit
# For example, you might run a linter or formatter

echo "Running pre-commit checks..."

# Example: Run a Python linter (e.g., flake8 or ruff)
# Ensure you have the linter installed in your environment
# if command -v ruff >/dev/null 2>&1; then
#   echo "Running ruff linter..."
#   ruff check .
#   if [ $? -ne 0 ]; then
#     echo "Ruff linter found issues. Commit aborted."
#     exit 1
#   fi
# else
#   echo "Ruff not found, skipping linting."
# fi

# Example: Run a Python formatter (e.g., black or ruff format)
# if command -v ruff >/dev/null 2>&1; then
#   echo "Running ruff formatter..."
#   ruff format .
#   git add . # Add any files formatted by ruff
#   if [ $? -ne 0 ]; then
#     echo "Ruff formatter failed. Commit aborted."
#     exit 1
#   fi
# else
#   echo "Ruff not found, skipping formatting."
# fi


echo "Pre-commit checks passed."
exit 0 